import React from 'react';

const propTypes = {};

const defaultProps = {};

const AboutApp = () => (
  <div>
    {' '}
    <h1>About App</h1>
  </div>
);

AboutApp.propTypes = propTypes;
AboutApp.defaultProps = defaultProps;

export default AboutApp;
